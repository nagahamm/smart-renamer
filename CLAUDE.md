# CLAUDE.md

## プロジェクト概要

macOSで撮影したスクリーンショットを `watchdog` で検知し、Vision framework のOCRとGemini APIでファイル名候補を生成、PyQt6のポップアップで選択させてリネーム・移動する常駐ツール。

監視による自動リネームに加え、`--rename` で任意のファイル・フォルダをその場でリネームする手動モードを持つ。

| ファイル | 役割 |
| --- | --- |
| `main.py` | ファイル監視、処理キュー、CLI、全体のフロー制御 |
| `ocr_engine.py` | Vision framework によるOCR、PDFのテキスト抽出とページ描画 |
| `llm_client.py` | Gemini APIへのファイル名候補リクエスト |
| `popup_ui.py` | PyQt6のリネーム候補ダイアログ、Qtイベントループの管理 |
| `config.yaml` | 全ての設定値と命名規則プロンプト |
| `setup.sh` | 仮想環境・`.env`・launchd常駐・Finderの右クリック登録のセットアップ |
| `requirements.txt` | 依存パッケージ。READMEと二重管理しない |
| `docs/` | READMEに貼るダイアログの画像と、その生成スクリプト（`render.py`） |

**設定値をコードにハードコードしないこと。** 監視先・保存先・タイムアウト・モデル名・命名規則は全て `config.yaml` に集約する。

## Communication Rules

- 敬語・挨拶・前置き・後置きは不要。
- 結論、変更内容、コードのみを最小限の言葉で伝える。

## Interaction & Execution Policy

- **Issueを起点に開発を進める。** 着手前にIssueを立て、症状・原因・対応方針を残してから実装する。バグ修正・機能追加・調査のいずれも対象。typoやフォーマット修正など、説明の必要がない些細な変更は除く。
- **作業中は確認しない。** 修正・テスト・コミットまでを一括で自律的に実行し切る。
- **質問してよいのは2つの場合のみ。** 着手前の前提確認と、予期せぬ破壊的変更のリスクがあり判断不能な場合。
- **`/compact` と `/clear` を活用する。** 細かい作業ごとではなく、文脈の異なる作業が5回程度重なった時点、または無関係なコンテキストが溜まった時点でリセットする。

## Git Commit Rules

Conventional Commits 規約（英語）に準拠する。

```
<type>(<scope>): <short summary>

- 変更点を箇条書き

Fixes #<issue番号>
```

対応するIssueを `Fixes #N`（バグ修正）または `Closes #N`（機能追加）で必ず閉じる。1つのIssueに複数コミットが紐づく場合は、最後のコミットにのみ書く。

### 粒度はタスク単位で決める

- **1コミット = 1タスク。** 「なぜこの変更をしたか」が1つの理由で説明しきれる単位に分割する。複数の目的を混ぜない。
- 性質の異なる変更（バグ修正 / リファクタリング / 新機能 / パフォーマンス / ドキュメント）は、同じファイルを触る場合でも必ず分ける。
- 同種のタスクや機械的に連動する変更（例: クラス名リネームに伴う複数ファイルの同時修正）は1コミットにまとめてよい。**ファイル単位ではなくタスク単位で判断する。**

### 順序と状態

- **各コミットは単体で成立させる。** どの時点でチェックアウトしても動作し、構文エラーが無い状態を保つ。「次のコミットで直る」中間状態を残さない。
- **バグ修正が先、リファクタリングが後。** 原因と対処がリファクタリングの巨大な差分に埋もれないようにする。
- **大きな作業は着手前に分割方針を決める。** 作業後にまとめてコミットして後から分割し直す手戻りを防ぐ。

### タグ

バグ修正・機能追加が `main` にマージされた時点で、`main` 上にタグを打つ。`vMAJOR.MINOR.PATCH` を使う。

**コミットの `type` ではなく、利用者から見た変化の大きさで決める。** ライブラリ的な厳密なSemVerではなく、一般的なアプリのリリース慣習に合わせる。`fix` が1件でも `feat` が混ざっていても、体感が変わらない範囲なら PATCH のままでよい。

- **PATCH**: バグ修正、内部改善、パフォーマンス改善、既存の挙動を安定させるための調整。使い方が変わらないものは全てここ。日常的なリリースはほぼこれになる。
- **MINOR**: 利用者が「新しくできるようになった」と分かる機能の追加、UIの変更。
- **MAJOR**: 全面刷新、または旧環境がそのままでは動かなくなる非互換変更（`config.yaml` の形式変更など）。

- 1コミット1タグにしない。`main` へマージした単位でまとめて1つのリリースとする。
- `docs` / `refactor` のみの変更ではタグを打たない。
- **利用者側の再設定が必要な場合**（`.plist` の書き換え、`config.yaml` への項目追加など）は、バージョンを上げる理由にはしないが、タグの注釈に必ず手順を明記する。
- 注釈付きタグで、含まれる変更とIssue番号を残す。

```
git tag -a v1.0.2 -m "Release v1.0.2: <利用者から見た変更点> (#1, #2, #3)"
git push origin v1.0.2
```

### `git commit --amend`

直前のコミットの修整・補足・関連作業であれば `--amend` で統合する。新規作業の着手時にこれに該当する場合は、作業前にその旨を提案する。別タスクであれば必ず新規コミットにする。

## Core Principles

- **SOLID**: 各モジュールの責務を上表の通りに保つ。OCR・LLM・UIの実装をまたいで混ぜない。
- **KISS**: 素直に書く。動く最小の実装を選ぶ。
- **YAGNI**: 今必要でない設定項目・抽象化・オプションを足さない。
- **DRY**: 同じロジック・同じ定数を二度書かない。既存の関数を再利用できるならそちらを使う。

## 開発環境の制約

このリポジトリはmacOS専用（Vision framework / PyObjC / PyQt6）。**動作確認できない箇所は推測で「確認済み」と書かず、実機での確認を促すこと。**

確認できる範囲は実行環境によって変わる。

- **Mac実機で作業している場合**: CLI・OCR・LLM呼び出し・launchd登録まで実際に動かせる。`QT_QPA_PLATFORM=offscreen` を使えばダイアログの描画も確認できる。動かせるものは動かしてからコミットする。
- **Linuxのサンドボックス上の場合**: macOS依存を含まないロジックは、依存モジュールをスタブ化すれば検証できる。検証可能なものは検証してからコミットする。

どちらの環境でも**自動化できない確認がある。** Finderの右クリックメニューへの表示、実ウィンドウのフォーカス挙動、システム設定での権限付与などはGUI操作が必要で、コマンドからは追い切れない。これらは「未確認」と明記して利用者に依頼する。
